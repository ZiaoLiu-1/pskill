package tui

// Key maps are now handled directly in app.go Update and per-tab Update methods.
// Global keys: q/ctrl+c (quit), 1-6 (tabs), tab/shift+tab (cycle)
// Tab-specific keys are defined in each tab's Update method.
